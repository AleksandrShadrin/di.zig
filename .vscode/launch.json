{
    "version": "2.0.0",
    "configurations": [
        {
            "type": "windbg",
            "request": "launch",
            "name": "Launch Program",
            "target": "${workspaceFolder}/zig-out/bin/zig.exe"
        },
        {
            "name": "Debug Zig with",
            "type": "by-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/zig-out/bin/zig",
            "cwd": "${workspaceFolder}",
        }
    ]
}